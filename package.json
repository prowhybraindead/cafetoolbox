{
  "name": "cafetoolbox",
  "private": true,
  "description": "CafeToolbox",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "clean": "turbo clean",
    "type-check": "turbo type-check",
    "monitoring:worker": "node scripts/health-check-worker.mjs",
    "monitoring:once": "node scripts/health-check-worker.mjs --once",
    "monitoring:aggregate-daily": "node scripts/aggregate-uptime-daily.mjs"
  },
  "devDependencies": {
    "dotenv": "^17.4.1",
    "prettier": "^3.5.0",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.8.0",
  "engines": {
    "node": ">=20"
  }
}
